# Unreleased

Add a bullet here in the same commit as any change that alters what a
`brew`-installed user sees or does. See docs/RELEASE-AUTOMATION.md.

- **Mermaid diagrams draw again** in the document window. In 1.2.0 every diagram
  showed as an empty gap, a regression from an upstream layout change; the fix has
  been submitted to Markdown Preview as well.
